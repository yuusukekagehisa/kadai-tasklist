<h1>タスクリスト一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to "編集", task %> :<%= task.status %> &gt; <%= task.content %></li>
  <% end %>
  
  <%= paginate @tasks %>
  
  <br><br><%= link_to '新規タスクの追加', new_task_path %>
  
</ul>